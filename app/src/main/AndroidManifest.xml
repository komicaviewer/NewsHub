<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="tw.kevinzhang.newshub.permission.BIND_EXTENSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="tw.kevinzhang.newshub.permission.BIND_EXTENSION" />

    <queries>
        <intent>
            <action android:name="tw.kevinzhang.newshub.extension.SERVICE" />
        </intent>
    </queries>

    <application
        android:name="tw.kevinzhang.newshub.App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Newshub"
        tools:targetApi="31">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name="tw.kevinzhang.newshub.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="locale"
            android:theme="@style/Theme.Newshub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="tw.kevinzhang.newshub.oauth"
                    android:host="callback" />
            </intent-filter>
        </activity>

        <!-- Public installed-app client id. Access/refresh tokens are never manifest metadata. -->
        <meta-data
            android:name="newshub.oauth.reddit.client_id"
            android:value="${redditOAuthClientId}" />
        <!-- OAuth 1 requires a consumer secret. It is injected at build time and never sent to
             extensions, but a mobile APK cannot make an application-level secret unrecoverable. -->
        <meta-data
            android:name="newshub.oauth1.plurk.consumer_key"
            android:value="${plurkConsumerKey}" />
        <meta-data
            android:name="newshub.oauth1.plurk.consumer_secret"
            android:value="${plurkConsumerSecret}" />
    </application>

</manifest>
