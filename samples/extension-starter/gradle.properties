org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# This path works while the starter remains under NewsHub/samples.
newshubDir=../..

# After NewsHub publishes protocol v2, pin one reviewed commit. Never use main or a range.
# newshubApiPin=replace_with_reviewed_commit_sha
